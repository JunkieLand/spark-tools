package com.renault.hercules.traceability

import java.io.PrintWriter

//import org.apache.derby.drda.NetworkServerControl

import scala.collection.mutable

object EmbeddedDerby {
  //  val derbyServer = new NetworkServerControl()
  //  val derbyState = mutable.Map[String, Boolean]()
  //
  //  def start() {
  //    if (!derbyState.getOrElse("running", false)) {
  //      EmbeddedDerby.derbyServer.start(new PrintWriter(System.err))
  //      derbyState += "running" -> true
  //    }
  //  }
  //
  //  def stop() {
  //    if (derbyState.getOrElse("running", false)) {
  //      EmbeddedDerby.derbyServer.shutdown()
  //      derbyState += "running" -> false
  //    }
  //  }

}
